export interface IJumper {
	hasJumper?: boolean
	code: string
	width: string
	hasDrawing?: boolean
}

export interface IMainJumper {
	id: string
	code: string
	hasDrawing: boolean
	imageUrl: string
}
